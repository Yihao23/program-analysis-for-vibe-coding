<!-- CONCEPT-MAP.zh.md -->
# 概念 → 代码（按讲义顺序）

本文件是 `program-analysis-for-vibe-coding` 与《Program Analysis》01–09 讲之间的对照表。README 只保留运行方法和场景说明；概念映射以这里为准。

每行四列：**讲义里的概念** · **它在讲义里说什么** · **映射到门禁**（讲义里的每个符号在门禁里是什么）· **代码位置**。

```
★  直接实现     门禁里有一段代码就是在做这件事
≈  类比         结构对应，但不是同一个数学对象
—  设计依据     影响了怎么写，没有对应代码
```

行号以 `gate/gate.cpp`、`specs/specs.cpp` 当前版本为准。

## 01 Introduction

| 概念 | 讲义里 | 映射到门禁 | 位置 |
|---|---|---|---|
| ★ Rice 定理 | 非平凡语义性质不可判定 | 没有"检查代码是否正确"这种检查。只有一条条具体性质，每个 `SPEC` 是一条 | `specs.cpp:50,59,97` |
| ★ `Init ⊆ I`，`Next(I) ⊆ I`，`I ∩ Bad = ∅` | 归纳不变式：起点在 I 里、走一步仍在 I 里、I 不含坏状态 | `Init` = 当前 `app/`；`Next` = agent 的一次改动；`I` = 全部检查的合取；`Bad` = 任一 ✗。`gate check` = 验证 `Next(I) ⊆ I` | `gate.cpp:276` |
| ★ 静态 vs 动态 | 过近似不漏 bug 可误报；欠近似不误报可漏 bug | UPPER 段 = 过近似类（对所有输入的断言）；LOWER 段 = 欠近似类（具体执行）。两者互补不替代 | `gate.cpp:288-297` |

## 02 Data-Flow Analysis

| 概念 | 讲义里 | 映射到门禁 | 位置 |
|---|---|---|---|
| ★ 抽象域 D | 分析能看见的信息种类 | 五个常量 = 门禁看得见的维度：目录信任级、include 关系、行数、逃生口字符串、tidy 规则。不在这些维度里的东西门禁是盲的 | `gate.cpp:29-53` |
| ★ I 必须是 D 的元素 | 不变式要能在域里表达 | "features 不碰 billing"这条默契写成 `Contract` struct 才存在；没写下的对 AI 不存在 | `gate.cpp:41` |
| ★ 跨分量的 I | 涉及多个变量的不变式 | 涉及多个文件的规则：写在伞头文件的 INVARIANT 块，由跨文件 SPEC 检查 | `users.hpp:2-5` ↔ `specs.cpp:50` |
| ≈ 单调 `a ⊑ b ⟹ f(a) ⊑ f(b)` | 输入越精确，输出越精确 | 给 agent 的上下文越精确，产出越可预测；也是"改→查→改"循环能收敛的前提 | CLAUDE.md |
| ≈ Kleene `lfp = fⁿ(⊥)` | 从 ⊥ 迭代到不再变化 | agent 改 → gate → 改 → … 直到 VERDICT"可合并"。这个循环就是 Kleene 迭代 | CLAUDE.md 协议第 3 步 |
| ★ `⊥` | 最精确 / 空状态 | `fresh()`：每个 SPEC 先清空全部全局状态 | `specs.cpp:40` |
| ≈ ACC | 升链必然稳定 | 检查体系必须有底：约束不能被无限放宽 —— 这就是禁逃生口的理由之一 | `gate.cpp:49` |

## 03a Worklist

| 概念 | 讲义里 | 映射到门禁 | 位置 |
|---|---|---|---|
| ★ `Dep_b` | 依赖于 b 的位置集合 | 反向 `#include` 图：谁 include 了 b | `gate.cpp:316-322` |
| ★ `W ≔ W ∪ Dep_b` 闭包 | 只重算受影响的位置 | `affected` 算改动的影响闭包（`.cpp` 先映射到同名 `.hpp`）。`check` 目前不用它剪枝，保守全跑 | `gate.cpp:325-331` |
| ≈ 出入度有界 → `O(h·|V|)` | 依赖稀疏，增量才便宜 | `K_MAX_INCLUDES` 限制每文件 include 数，让影响范围可控 | `gate.cpp:46` |

## 03b Widening / Narrowing

| 概念 | 讲义里 | 映射到门禁 | 位置 |
|---|---|---|---|
| — widening 公理 `d₁ ⊔ d₂ ⊑ d₁ ∇ d₂` | 跳得只多不少，sound | agent 快速产出泛化的解 = widening：能用，但打过头 | — |
| ★ 阈值集 K | 跳到程序里的常数，不逐格协商 | 函数 ≤ 40 行、include ≤ 6：给 agent 的约束是阈值，不是"看情况" | `gate.cpp:46` → `check_K` (187) |
| ≈ narrowing 是 anytime 的 | 从上往下，每步都 sound，随时可停 | 生成完跑 reduce 去冗余、简化；做一步停下也 sound，不必一次做完 | CLAUDE.md"完成之后" |

## 04 Abstract Interpretation I

| 概念 | 讲义里 | 映射到门禁 | 位置 |
|---|---|---|---|
| — 具体语义 `f(Δ)` | 真实状态集合上的一步 | agent 改动对程序**真实行为**的影响。不可计算，但它是所有 sound 论证的锚点 | — |
| ≈ Galois `l ⊑ γ(α(l))` | 抽象再具体化，比原物粗 | 门禁对代码的了解 = 声明区那几个维度，永远比代码粗。"理解 AI 代码"= 在关心的维度上足够精确，不是读每一行 | `gate.cpp:29-53` |
| ★ α vs spec | α 从代码算出；spec 不在代码里 | `app/` 只有 α（实际做什么）；`specs/` 是 spec（应该做什么）。检查 = α(code) 满足 spec | `dedupe.hpp:2-3` ↔ `specs.cpp` |
| ★ 上近似 `f(γ(m)) ⊑ γ(f#(m))` | 抽象走一步必须包住具体走一步 | `α` = `check()` 从代码算出报告；`m` = 基线报告；`γ(m)` = **所有会产生同一份报告的代码库**（门禁分辨不出的一切，无代码，由盲区清单描述）；`f` = 改动作用在真实代码上；`f#` = 改动后再跑 `check()`。**检查范围必须覆盖改动的全部影响**：文件局部性质看 `scope`，跨文件性质扫全部，行为类全跑 | `gate.cpp:277-286`；详见下节 |
| ★ 不动点转移 `lfp(f) ⊑ γ(lfp(f#))` | 每步局部 sound ⟹ 整体 sound | `f` = 一个 PR；`fⁱ(⊥)` = 第 i 个 PR 后的真实代码库；`lfp(f#)` = 门禁累积的结论。每个 PR 单独过门禁 ⟹ 累积的"绿"对整个代码库成立，不需要全局审查。前提：每一步 f 都有对应的 f#，即**没有 PR 不走门禁就合并**（这由合并策略守，demo 未实现）。注意：现在的 `check()` 全量重算，报告不跨 PR 携带，此定理暂时没有牙；开始用 `affected()` 剪枝或依赖信任标签时它才是硬前提 | `gate.cpp:276` |
| — 最佳 `f# = α∘f∘γ` | 最精确的上近似，γ 无限所以不可实现 | 理想的审查 = 展开改动的全部影响再压回检查体系。门禁是它的粗近似 | — |

## 05 Abstract Interpretation II

| 概念 | 讲义里 | 映射到门禁 | 位置 |
|---|---|---|---|
| ★ 约化算子 `ρ = α∘γ` | 同一具体值的多个表示压到最小 | `fingerprint`：参数名 → `_argN`、去空白；指纹相同的两个函数 = 同一行为的冗余表示 | `gate.cpp:201, 216` |
| ★ 不约化则直线代码也丢精度 | 腐烂的数学形式 | `dedupe_dup`：功能全对，只是多了一个表示。每次完成后跑 reduce，而不是季度末重构 | `candidates/dedupe_dup.hpp` |
| ≈ 独立属性法丢关系 | 逐分量抽象，丢掉分量间约束 | 逐文件审查 = 独立属性法：丢掉跨文件关系 | — |
| ★ 关系法 | 一起抽象，保住约束 | 跨文件 SPEC（write → read 的复合结果） | `specs.cpp:50` |
| ≈ 顺序复合 | 抽象叠抽象仍是抽象 | 类型 → include 契约 → 信任分区，逐层；审上层不必看下层 | — |

## 06 Logic / SMT

| 概念 | 讲义里 | 映射到门禁 | 位置 |
|---|---|---|---|
| ★ `F 有效 ⟺ ¬F 不可满足` | 求解器只找反例 | 所有检查都是找反例；"没找到"就是你能得到的全部保证 | `specs.cpp:97-111` |
| ≈ 去掉表达力换可判定性 | 无乘法、无量词才可判定 | 每条约束都是受限的（行数、字符串、include 前缀），所以才能机械判定。"代码要优雅"不可判定，所以不在列表里 | `gate.cpp:29-53` |
| ≈ 理论 = 签名 + 公理 | 在公理内推理 | `CONTRACTS` 每条是一条公理；agent 的产出在公理下是否有效是可判定的 | `gate.cpp:41` |

## 07 Symbolic Abstraction

| 概念 | 讲义里 | 映射到门禁 | 位置 |
|---|---|---|---|
| ★ 最佳局部复合出糟糕全局 | swap 三步各最佳，整体爆炸 | 两个 session 各自"最佳"（Session 1 加缓存，Session 2 改写操作），复合坏了。检查复合结果，不是单步 | `specs.cpp:50`，场景 5 |
| ≈ γ̂ 符号具体化 | 用公式表示无限集合 | 一条 `SPEC(upper, …)` = 用代码表示"对所有输入"的性质 | `specs.cpp:97` |
| ★ RSY：只有下界 | 求解器返回某一个模型，不是你要的 | 只给示例时，agent 给出满足示例的最小程序（`std::set`） | 场景 1 |
| ★ 反例 S 与 β(S) | 反例泛化后并入下界 | 随机找反例、`shrink` 收缩；agent 把它当下一版必须处理的输入 | `specs.cpp:98-108` |
| ★ Bilateral：lower 抬、upper 收 | 双向逼近，相遇即停 | `lower` = LOWER 段（已确认必须能）；`upper` = UPPER 段（已确认不能越）；`mid` = agent 的提案；求解器 = 门禁；收敛 = "上界 ✓ 下界 ✓" | `gate.cpp:288-297` |
| ★ `l ⊑ AC(l,u) ∧ u ⋢ AC(l,u)` | 每次的 mid 必须让某一侧严格进展 | `l ⊑ mid`：提案不退步 → LOWER 全部重跑；`u ⋢ mid`：提案不能是 ⊤ 的伪装 → 禁逃生口（NOLINT = 不做任何承诺的提案）；"无论哪种回答都有进展" → 每个 ✗ 必带 `detail`（= S） | `gate.cpp:279, 296`, `Result.detail`；详见下节 |
| ★ 超时返回非平凡上界 | Bilateral 优于 RSY 之处 | 人来不及审 = 超时。机械上界仍成立 → VERDICT"上界 ✓ 下界 ✗ → 等人审"，而不是一无所知 | `gate.cpp:305-310` |
| ★ 生成者针对检查优化 | 求解器给最省事的模型 | 逃生口 = 让检查变绿的最短路径；对 AI 一律禁止 | `gate.cpp:49, 177` |
| ★ 误报变便宜 | 精度/代价的最优点移动 | 重生成几乎免费 → 开对人不敢开的检查：`-Werror` 全开、tidy 警告即错误 | `CMakeLists.txt:8`, `gate.cpp:53` |
| ≈ 无循环片段整体分析 | 整段符号执行保住关系 | 审意图粒度而非 diff 粒度：跨文件 SPEC | `specs.cpp:50` |

## 09 Information Flow

| 概念 | 讲义里 | 映射到门禁 | 位置 |
|---|---|---|---|
| ★ 完整性格 | 信息不得从 untrusted 流向 trusted | `TRUSTED = app/core`（人审过），`UNTRUSTED = app/features`（AI 产出，未经人审）。报告标 tier | `gate.cpp:33-34` |
| ★ 出处成安全等级 | — | 未经审查的 AI 产出 = untrusted。等级描述的是"有没有人担保"，不是质量 | CLAUDE.md 信任分区 |
| ★ 显式流 | 经数据流 / 依赖 | `#include` 契约 | `gate.cpp:41, 167`，场景 3 |
| ★ 隐式流 / 上下文 G | 经控制而非赋值 | 前置声明改全局 `settings()`：include 图上不存在这条边，但 billing 行为变了 | `billing.hpp:4` ↔ `promo_extern.cpp`，场景 4 |
| ★ 非干扰 `M₁ =_L M₂ ⟹ P(M₁) =_L P(M₂)` | 低变量相同的两个初态，终态低变量仍相同 | `M₁` = 不链 features 的世界，`M₂` = 链的世界；`=_L` = trusted 部分相同；`P(·)` = 账本。两份账本必须一致 | `CMakeLists.txt:13,24-25` + `golden.cpp` + `gate.cpp:258` |
| ★ 超性质 | 关于 trace **集合**的性质，单条看不出 | HYPER 单独成段：不能靠单次测试，必须构建两个世界 | `gate.cpp:297` |
| ★ OBJECT 库 | — | 静态库会丢掉未引用的 .o 连同静态初始化器；OBJECT 保证隐式流能被观测 | `CMakeLists.txt:12-13` |

未用到的：08 讲（符号 / concolic 执行）。它是找触发输入的技术，可以给 agent 写的代码自动扩测试集，本 demo 没做。

## 三条真正承重的

```
Next(I) ⊆ I                     gate.cpp:276   check()
f(γ(m)) ⊑ γ(f#(m))              gate.cpp:277   scope + affected()
l ⊑ AC(l,u) ∧ u ⋢ AC(l,u)       gate.cpp:288   upper / lower 两段
```

## 关于 `f(γ(m)) ⊑ γ(f#(m))` 在门禁里的含义

先把每个符号对上：

```
α           check()：从代码算出报告                               gate.cpp:276
m           基线报告（全 ✓）
γ(m)        所有会产生这份报告的代码库 —— 门禁分辨不出的一切        没有代码；"已知盲区"一节描述的就是它
f           agent 的改动作用在真实代码上
f(γ(m))     对 γ(m) 里每个程序施加这次改动，得到的所有可能结果
f#          改动之后再跑一次 check()
γ(f#(m))    所有会产生新报告的代码库
```

一份绿色报告说的不是"这个程序是对的"，而是"这个程序属于 γ(m)，集合里每个程序都过了这些检查"。
γ 永远不可计算（04/07 讲），但可以被描述：它就是门禁不看的一切。每补一条检查，γ(m) 缩小一圈 —— γ 的大小就是门禁的精度。

这条不等式说：抽象地算一步，结果必须覆盖具体地算一步的所有可能。
在门禁里，"具体的一步"是 agent 的改动对程序真实行为的影响，"抽象的一步"是门禁跑的检查。
不等式成立 ⟺ **检查的范围覆盖了改动的全部影响**。它在代码里体现为三个范围决定：

```
gate.cpp:277   scope = 改动的文件          arch / escape / K 只看这些  —— 这些性质是文件局部的，改动的影响不出文件
gate.cpp:281   reduce 永远扫整个 app/      —— 重复是跨文件性质，只看改动文件会漏掉与别处的重复
gate.cpp:283-285  specs / hyper 永远全跑    —— 行为影响可以跨到任何地方，保守地全覆盖
```

`affected()` 算的是"最小的仍然覆盖全部影响的范围"。`check()` 现在没用它来剪枝，而是保守地全跑；
如果要用它剪枝，前提是依赖图**过近似**真实影响 —— 而 `#include` 图不满足这一点（场景 4 的前置声明
不在图里），所以 HYPER 永远不能按 `affected()` 剪。这就是不等式在实际工程里的形状：
**每一次"这次可以少跑一点"的决定，都是在赌 f# 仍然覆盖 f。**

## 关于 `l ⊑ AC(l,u) ∧ u ⋢ AC(l,u)` 在门禁里的含义

07 讲 Bilateral 算法每轮取一个 `mid`，要求 `l ⊑ mid`（包住已确认的下界）且 `u ⋢ mid`（严格比上界窄）。
两条合起来保证：无论求解器回答"有反例"还是"没有"，lower 或 upper 必有一侧严格进展，循环不会原地打转。

映射到门禁，`mid` 是 agent 的提案，求解器是门禁，循环是"提案 → 报告 → 改 → …"：

```
l ⊑ mid      提案不能退步         gate.cpp:296   LOWER 段每次全部重跑，不按改动剪枝
u ⋢ mid      提案不能是 ⊤ 的伪装  gate.cpp:279   禁逃生口：加 NOLINT 的提案不做任何承诺，
                                                 门禁对它永远说"没反例"，upper 零进展
必有一侧进展  每个 ✗ 必须带反例    Result.detail  of() 只在 violations 非空时 FAIL；specs 失败给 FAIL 行；
                                                 hyper 失败给两份账本。门禁从不说"不过，但不告诉你为什么"
```

诚实的说明：讲义里 `mid` 由算法算，这里由 agent 提。门禁能检测退步（LOWER 某行 ✗）、能堵已知的伪装（逃生口列表），
但不能阻止 agent 提出退步方案，也堵不住未知的伪装。进展的保证一半在门禁（每次回答带反例），一半在 agent（拿反例去改而不是换伪装）。
CLAUDE.md 的"报错就修代码、不要绕"是在补 agent 那一半。

