# 工作流：让 AI agent 写代码，用门禁保持可控

配套：`CONCEPT-MAP.zh.md`（概念 ↔ 代码）· `README.zh.md`（demo 运行）

这份文档只回答一个问题：**代码由 agent 生成时，人、agent、门禁三方各做什么，按什么顺序，每一步背后是讲义里的哪个概念。**

---

## 0. 三个角色

```
   人        选择抽象域（决定看什么）· 写 spec（决定要什么）· 审 trusted 区的改动
   agent     在 spec 和约束之下生成代码 · 拿门禁的反例修 · 不碰抽象域
   门禁      对每次改动回答 Next(I) ⊆ I ?  · 每个 ✗ 必须带反例 · 从不替人判断"对不对"
```

---

## 1. 建项目：先立门禁，再写第一行业务代码

```
① CLAUDE.md          写下协议和信任分区的打算。此时没有代码，只有意图
② 声明区             gate 顶部五个常量：TRUSTED / CONTRACTS / K / ESCAPE_HATCHES / TIDY_ARGS
                     检查函数可以先是空的 —— 先把"看什么"定下来
③ specs/ 骨架        框架 + 至少一条 lower、一条 upper；有跨模块行为就加 hyper 观测器
④ gate check 全 ✓    对空项目跑通
⑤ 然后才有 app/
```

顺序不能反：I 必须先存在，`Next(I) ⊆ I` 才有意义。先写代码再补门禁，第一次 check 就是一堆 ✗，你会被迫放宽 I 去迁就代码 —— 抽象域被代码定义了，而不是反过来。

---

## 2. 每个功能的循环

```
   人      写 spec：SPEC(lower, …) 说"必须能"，SPEC(upper, …) 说"绝不能"
            │        跨模块的关系写进模块入口的 INVARIANT 块 + 一个跨文件 spec
            ▼
   agent   读 CLAUDE.md、声明区、相关 INVARIANT 块 → 实现
            │
            ▼
   门禁    gate check <改动的文件>
            │
            ├─ 上界 ✓ 下界 ✓   → untrusted 区：合并。trusted 区：等人审
            ├─ 上界 ✗          → 反例给 agent，回到 agent。不需要人
            └─ 上界 ✓ 下界 ✗   → agent 没做到要求的事：换 prompt 或人补 spec，回到人
```

### 报告长什么样

`gate check` 的输出就是"报告"。基线（全 ✓）：

```
GATE REPORT     Next(I) ⊆ I ?
UPPER  约束 —— 绝不能
  ✓ build
  ✓ arch
  ✓ escape
  ✓ K
  ✓ reduce
  ✓ clang-tidy
  ✓ upper        1 passed, 0 failed
LOWER  示例 —— 必须能
  ✓ lower        2 passed, 0 failed
HYPER  超性质
  ✓ noninterf    两个世界的账本一致: 10.00 2.68 0.12 100.00
VERDICT  上界 ✓ 下界 ✓ → 可合并
```

同一个门禁，换一份代码（场景 4：features 用前置声明改了全局取整）：

```
GATE REPORT     Next(I) ⊆ I ?
  改动: app/features/promo.cpp   [untrusted]
UPPER  约束 —— 绝不能
  ✓ build
  ✓ arch                                   ← #include 检查看不见前置声明
  ✓ escape
  ✓ K
  ✓ reduce
  ✓ clang-tidy
  ✓ upper        1 passed, 0 failed
LOWER  示例 —— 必须能
  ✓ lower        2 passed, 0 failed
HYPER  超性质
  ✗ noninterf    features/ 改变了 billing 的行为（隐式流）:
                   不链接 features: 10.00 2.68 0.12 100.00
                   链接 features:   10.00 2.67 0.12 99.99
VERDICT  上界 ✗ 下界 ✓ → 做到了要求的事，但越界了（拒绝，不需要人审）
```

怎么读：

```
报告          = 门禁对代码的全部了解 = α(code) = 抽象域里的一个点
每一行        = 抽象域的一条轴，值是 ✓ 或 ✗ + 细节
✗ 后面的细节  = 反例 —— agent 拿它去改，不需要人翻译
VERDICT       = 上界（UPPER + HYPER）和下界（LOWER）各自有没有 ✗ 的组合
```

除了这十一行以外的一切（变量叫什么、用了什么算法、有没有没人调用的函数）都不在报告里 —— 那是门禁分不清的部分，也就是 γ。

---

## 3. 四种 VERDICT 分别做什么

```
上界 ✓ 下界 ✓    untrusted 区：合并        trusted 区：人审后合并
上界 ✓ 下界 ✗    agent 没做到要求的事。先看 spec 写清楚没有，再决定换 prompt 还是补 spec
上界 ✗ 下界 ✓    做到了但越界。反例给 agent，不需要人 —— 这一格是把人从瓶颈里挪出来的关键
上界 ✗ 下界 ✗    拒绝。通常是 agent 误解了任务，重写 prompt
```

---

## 4. 每一步背后的概念（简单版）

每一行：**工作流里的一步 → 讲义里的概念 → 用一句大白话说它为什么在这里**。

### 建项目

| 步骤 | 概念 | 大白话 |
|---|---|---|
| ① 先写 CLAUDE.md | 01 讲 Rice 定理 | 没有"检查代码对不对"这种东西，只能检查一条条具体的事。所以先想清楚要检查什么 |
| ② 声明区五个常量 | 02/05 讲 抽象域 | 你决定看哪几个维度，就只能看见这几个维度。这是人替项目做的取舍，agent 不该做 |
| ② 里的 CONTRACTS | 02/03 讲 不变式要能在域里表达 | 没写下来的规则，对 agent 等于不存在。写成机器读的形式它才存在 |
| ② 里的 TRUSTED / UNTRUSTED | 09 讲 完整性格 | 有人担保过的代码和没人看过的代码是两个等级，后者不能影响前者 |
| ② 里的 K | 03b 讲 阈值集 | 给 agent 的约束是"不超过 40 行"这种硬数字，不是"看情况"。硬数字收敛得快 |
| ② 里的 ESCAPE_HATCHES | 07 讲 生成者会针对检查优化 | agent 的目标是让门禁变绿。NOLINT 是最短路径，所以要堵死 |
| ③ specs 先于 app | 04 讲 α vs spec | 代码只能告诉你它做了什么，永远不能告诉你这是不是你想要的。"想要的"必须另外写 |
| ③ 一条 lower + 一条 upper | 07 讲 Bilateral | 示例说"至少要能做到这个"，性质说"绝不能超出这个"。只有一边，agent 会钻空子 |
| ③ hyper 观测器 | 09 讲 超性质 | 有些事一次运行看不出来（"A 有没有偷偷影响 B"），要跑两次比对 |
| ④ 空项目跑到全 ✓ | 02 讲 Init ⊆ I | 起点必须在规则之内，之后每一步才能问"还在不在规则之内" |

### 公式里每个符号是什么

上面表里出现的公式，逐个符号对到工作流里的东西。

**`Next(I) ⊆ I`**（gate check）

```
I         门禁检查的全部条件合起来 —— "健康状态"
Next      agent 的一次改动
Next(I)   健康状态经过这次改动之后变成的样子
⊆ I       仍然健康
```

**`l ⊑ γ(α(l))`**（agent 先读声明区）

```
l         真实的代码
α         门禁：从代码算出一份报告
α(l)      报告
γ         反过来：一份报告允许的所有代码
γ(α(l))   和现在这份代码"在门禁眼里一样"的所有代码
l ⊑ …     真实代码是其中之一 —— 门禁看到的永远比真实的粗
```

**`f(γ(m)) ⊑ γ(f#(m))`**（gate check 全跑行为类检查）

```
m           改动前的报告（全 ✓）
γ(m)        改动前，门禁分不清的那一堆代码（真实代码是其中之一）
f           agent 的改动，作用在代码上
f(γ(m))     对那一堆代码每一份都做这个改动，得到的所有可能结果
f#          改动之后再跑一次门禁
f#(m)       改动后的报告
γ(f#(m))    改动后的报告允许的所有代码
⊑           左边的每一种可能结果，都必须在右边允许的范围里
            —— 翻译：改动可能造成的每一种后果，新报告都得看得见。
               所以检查的范围不能比改动的影响小，行为类检查不能只跑一部分
```

**`l ⊑ AC(l,u) ∧ u ⋢ AC(l,u)`**（agent 的每次提案）

```
l           已确认"必须能做到"的那些事（lower：示例 spec）
u           已确认"绝不能越过"的边界（upper：约束 + 性质）
AC(l,u)     agent 这一轮交上来的提案
l ⊑ 提案    提案不能把已经做到的事弄丢 —— 所以 lower 每次全跑
u ⋢ 提案    提案必须比边界真的窄一点，不能是"什么都不承诺"—— 所以禁逃生口
∧           两条同时成立，这一轮才一定有进展
```

**`lfp = fⁿ(⊥)`**（agent 拿反例改，再跑）

```
⊥           从零开始
f           改一轮 + 查一轮
fⁿ(⊥)       改查 n 轮之后的状态
lfp         不再变化的那个状态 —— VERDICT"可合并"
```

**`ρ = α∘γ`**（完成后跑 reduce）

```
γ           一个函数 → 它实际做的事
α           实际做的事 → 最短的那个写法
ρ           把每个函数换成做同一件事的最短写法
            —— 两个函数 ρ 之后相同 = 它们是重复的
```

**`M₁ =_L M₂ ⟹ P(M₁) =_L P(M₂)`**（hyper 观测器）

```
M₁, M₂      两个世界：一个不带某个模块，一个带
=_L         在"可信部分"上相同
P(·)        跑完之后可观测的结果
⟹          可信部分一样，结果就必须一样 —— 那个模块不许偷偷影响别人
```

---

## 5. 一页版

```
建项目    CLAUDE.md → 声明区 → specs 骨架 → gate 全 ✓ → 才写代码
每个任务  人写 spec → agent 实现 → gate check → 按 VERDICT 分流
不动的    声明区只有人能改；逃生口一律禁；行为类检查永远全跑
```
